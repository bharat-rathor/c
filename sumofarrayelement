#include<stdio.h>
main()
{
    int arr[5],sum=0;
    for(int i=0;i<=5;i++)
    {
        printf("Enter Your Element : ");
        scanf("%d",&arr[i]);
        sum=sum+arr[i];
    }
    printf("Sum Is %d",sum);
}